1. install the new dependencies in frontend and backend - DONE
2. Create the routes in app.jsx - DONE
    "/" which will show a login page 
    "/contacts" which will show the main contacts app (private page)
    "/register" which will show a registration page
    "/not-authorized" which sill show a 403 error if a user is not autorized and wants to enter /contacts - DONE
    "*" if the route does not exist
3. User model to be stored on a new collection/table on our contacts DB
4. On the backend, we need to handle all the login/registration 
5. On frontened, we need to create a login page, a registration page, a not-authorized page, and a not-found page - DONE
6. We need to edit ContactsApp to show the username at the top of the page